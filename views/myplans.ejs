<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>7odafone</title>
  <style>
 @import url("https://fonts.googleapis.com/css?family=Roboto:400,400i,700");
 @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@100;300;400;500;600;700;800;900&display=swap');

*{
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: Roboto, sans-serif;
  }
  
  body {
    height: 100vh;
    background: #1e1b1b;
    display: grid;
    grid-template-columns: 25% 70%;
  }
  .cont{
  width:200px;
  }
  .main{
    width:100%;
    height:100%;
  }
  .carousel{
  display:flex;
  flex-direction: row;
  width:100%;
  height:100%;
  justify-content: center;
  align-items: center;
  margin-left:-5%;
  }
  .headers{
  display:flex;
  justify-content: space-between;
  color:#c7fffb;
  padding:10px;
  }
  .wel{
    margin-left:30%;
  }
  .logo{
   color:#645e5e;
   font-weight: bolder;
   letter-spacing: 2px;
  }

  .arrow{
    color:#c7fffb;
    font-size:40px;
    margin-top:-5%;
  }
  .left-arrow{
    margin-left: -7%;
  }
  .right-arrow{
    margin-right: -7%;
  }
  li{
    color:#c7fffb;
  }
  .nav {
    background: #2e2a2a;
    padding: 20px 0 10px 10px;
    border-right: 5px solid #c7fffb;
    width: 150px;
    height:100vh;
    transition: 0.5s;
    overflow: hidden;
  }
  
  .nav .nav-elem {
    list-style: none;
    padding: 15px;
    padding-right: 0;
    color: #effefd;
    font-size: 15px;
    margin-bottom: 10px;
    cursor: pointer;
    position: relative;
    transition: 0.5s;
    border-top-left-radius: 20px;
    border-bottom-left-radius: 20px;
    font-weight: bold;
    display: flex;
    justify-content: center;
    letter-spacing: 1px;
    padding-right:10px;
  }
  
  .nav .nav-elem:hover {
    transition: 0s;
    background: #1c1f24;
  }
  
  .nav .nav-elem.active:before {
    content: '';
    position: absolute;
    top:-20px;
    right: 0px;
    width: 20px;
    height:20px;
    background: transparent;
    border-radius: 50%;
    box-shadow: 10px 10px 0 #c7fffb;
  }
  
  .nav .nav-elem.active:after {
    content: '';
    position: absolute;
    bottom:-20px;
    right: 0px;
    width: 20px;
    height:20px;
    background: transparent;
    border-radius: 50%;
    box-shadow: 10px -10px 0 #c7fffb;
  }
  
  .nav .nav-elem.active {
    background: #c7fffb;
    color:#242932;
  }
  
  .nav:hover {
    width: 300px;
  }
  
  .title{
    margin-bottom: 60px;
    color: #c7fffb;
  }
  .items{
    width: 100%;
    height:70%;
    background: transparent;
    display:flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }
#plan{
   font-size:25px;
   color:#c7fffb;
   margin-bottom:10%;
}
.container {
  width: 100%;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 80px 100px;
  overflow: hidden;
  background: transparent;
}

.box {
  position: relative;
  width: 240px;
  height: 250px;
  display: flex;
  justify-content: center;
  align-items: center;
}

.box::before {
  content: '';
  position: absolute;
  top: 0;
  left: 0;
  width: 20px;
  height: 100%;
  background: transparent;
  filter: blur(1px);
  z-index: 1;
}

.box::after {
  content: '';
  position: absolute;
  top: 1px;
  right: -1px;
  width: 20px;
  height: 100%;
  background:transparent;
  filter: blur(1px);
  z-index: 1;
}

.box .shadow {
  position: absolute;
  width: 100%;
  height: 100%;
  background:tranpsarent;
}

.box .shadow::before {
  content: '';
  position: absolute;
  top: 0;
  left: calc(100% + 5px);
  width: 100%;
  height: 200%;
  background: linear-gradient(rgba(0,0,0,0.075), transparent);
  transform: skewX(45deg);
}

.box .shadow::after {
  content: '';
  position: absolute;
  bottom: -200%;
  left: calc(100% + 15px);
  width: 100%;
  height: 200%;
  background: linear-gradient(rgba(0,0,0,0.075), transparent);
  transform: skewX(45deg);
}

.box .content {
  position: relative;
  width: 100%;
  height: 100%;
  background-color: transparent;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
}

.box .content .percent {
  position: relative;
  width: 150px;
  height: 150px;
}

.box .content .percent::before {
  content: attr(data-text);
  position: absolute;
  inset: 20px;
  background: #555;
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
  color: #fff;
  font-size: 1.75rem;
  letter-spacing: 0.1rem;
  text-transform: uppercase;
}

.box .content .percent svg {
  position: relative;
  width: 150px;
  height: 150px;
  transform: rotate(270deg);
}

.box .content .percent svg circle {
  width: 100%;
  height: 100%;
  fill: transparent;
  stroke-width: 3;
  stroke: rgba(0,0,0,0.05);
  transform: translate(5px, 5px);
}

.box .content .percent svg circle:nth-child(2) {
  stroke: #c7fffb;
  stroke-dasharray: 440;
  stroke-dashoffset: calc(440 - (440 * var(--num)) / 100);
  opacity: 0;
  animation: fadeIn 1s linear forwards;
  animation-delay: 2.5s;
}

@keyframes fadeIn {
  0% {
    opacity: 0;
  }
  100% {
    opacity: 1;
  }
}

.box .content .percent .dot {
  position: absolute;
  inset: 5px;
  z-index: 10;
  animation: animateDot 2s linear forwards;
}

@keyframes animateDot {
  0% {
    transform: rotate(0deg);
  }
  100% {
    transform: rotate(calc(3.6deg * var(--num)));
  }
}

.box .content .percent .dot::before {
  content: '';
  position: absolute;
  top: -7px;
  left: 50%;
  transform: translateX(-50%);
  width: 14px;
  height: 14px;
  background: #95bbb9;
  border-radius: 50%;
}

.box .content .number {
  position: relative;
  inset: 0;
  opacity: 0;
  animation: fadeIn 1s linear forwards;
  animation-delay: 2.5s;
}

.box .content .number h2 {
  font-size: 2.5rem;
  color: #c7fffb;
}

.box .content .number h2 span {
  font-weight: 300;
  font-size: 1.5rem;
}
    </style>
</head>
<body>
  <div class="cont">
    <ul class="nav">
      <a href="/customer" class=" nav-elem">Home</a>
      <a href="/wallet" class=" nav-elem">My Wallet</a>
      <a href="/plans" class=" nav-elem">Offered Plans</a>
      <a href="/benefits" class=" nav-elem">My Benefits</a>
      <a href="/shops" class=" nav-elem">Shops</a>
      <a href="/myplans" class=" nav-elem active">Plan Search</a>
    </ul>
  </div>
  <div class="main">
  <div class="headers">
    <h2 class="wel"> Welcome <%= name %></h2>
    <div class="logo">7ODAFONE</div>
  </div>
  <div class="carousel">
  <div class="left-arrow arrow" onclick="shift('left')"><</div>
  <div class="items">
    <div id="plan"></div>
    <div class="container"></div>
  </div>
  <div class="right-arrow arrow" onclick="shift('right')">></div>
 </div>
 <form method="post">
  <div class="wrapper">
   <h2>Enter</h2>
   <div class="space"></div>
   <h2>Date</h2>
  </div>
  <div class="input-container"> 
    <input class="inputs" placeholder="Plan Name" type="text" name="plan" id="plan" required>
  </div>
  <div class="input-container"> 
    <input class="inputs" placeholder="Start Date" type="date" name="startDate" id="start" required>
  </div>
  <div class="input-container">
    <input class="inputs" placeholder="End Date" type="date" name="endDate" id="end" required>
  </div>
  <button type="submit">Find</button>
  </form>
  </div>

  <script>
    let planNames=['Basic','Standard','Premium'];
    let items = document.querySelectorAll('li');
    items.forEach(item => {
      item.addEventListener('click', () => {
         items.forEach(item => item.classList.remove('active'))
          item.classList.add('active')
      })
    })

    let displayInd=0
    const values2 = '<%- JSON.stringify(usages) %>';
    const values = JSON.parse(values2)

  shift('none');
    function shift(direction){
  if(direction==='left'){
    if(displayInd===0)
    displayInd=9;
    else
   displayInd--;
  }
  else if (direction==='right'){
      if(displayInd===values.length-1)
      displayInd=0;
      else
     displayInd++;
  }
  else
    displayInd=0;
  createAndUpdateBoxes(values[displayInd]);
}

function createAndUpdateBoxes(values) {
  const container = document.querySelector('.container');
  const container2 = document.getElementById('plan');
  // Clear any existing boxes in the container
  // Loop through the values and create boxes dynamically
  container2.innerHTML=values.name;
  container.innerHTML="";
  container.innerHTML += `
      <div class="box">
        <div class="shadow"></div>
        <div class="content">
          <div class="percent" data-text="SMS" style="--num: ${values.minutes_used}">
            <div class="dot"></div>
            <svg>
              <circle cx="70" cy="70" r="70"></circle>
              <circle cx="70" cy="70" r="70"></circle>
            </svg>
          </div>
          <div class="number">
            <h2>${values.minutes_used}<span>%</span></h2>
          </div>
        </div>
      </div>
        <div class="box">
        <div class="shadow"></div>
        <div class="content">
          <div class="percent" data-text="Data" style="--num: ${values.data_consumption}">
            <div class="dot"></div>
            <svg>
              <circle cx="70" cy="70" r="70"></circle>
              <circle cx="70" cy="70" r="70"></circle>
            </svg>
          </div>
          <div class="number">
            <h2>${values.data_consumption}<span>%</span></h2>
          </div>
        </div>
      </div>
        <div class="box">
        <div class="shadow"></div>
        <div class="content">
          <div class="percent" data-text="Mins" style="--num: ${values.SMS_sent}">
            <div class="dot"></div>
            <svg>
              <circle cx="70" cy="70" r="70"></circle>
              <circle cx="70" cy="70" r="70"></circle>
            </svg>
          </div>
          <div class="number">
            <h2>${values.SMS_sent}<span>%</span></h2>
          </div>
        </div>
      </div>
    `;
}
    </script>
</body>
</html>